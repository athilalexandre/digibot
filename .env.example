# Variáveis de ambiente para o Digibot

# Configurações da Twitch
TWITCH_USERNAME="" # Nome de usuário do seu bot na Twitch
TWITCH_PASSWORD="" # Senha da conta do bot (se aplicável, ou deixe vazio se usar apenas OAuth)
TWITCH_OAUTH_TOKEN="" # Seu token OAuth da Twitch (EX: oauth:abcdef1234567890)
TWITCH_CHANNEL="" # Nome do canal que o bot deve monitorar (EX: seu_canal_aqui)

# Configurações do MongoDB
MONGODB_URI="mongodb://localhost:27017/digibot" # URI de conexão com o MongoDB (pode ser local ou remoto)

# Configurações da API
API_PORT=3000 # Porta em que a API do bot será executada

# Credenciais da Aplicação Twitch (para uso futuro, se necessário para outras interações com a API)
TWITCH_CLIENT_ID="" # ID do cliente da sua aplicação Twitch
TWITCH_CLIENT_SECRET="" # Segredo do cliente da sua aplicação Twitch
TWITCH_REFRESH_TOKEN="" # Token de refresh para a sua aplicação Twitch

# Lembre-se: O arquivo .env NUNCA deve ser enviado para repositórios públicos (ex: GitHub).
# O .env.example serve como um modelo. Crie um arquivo .env com suas credenciais reais e mantenha-o privado.